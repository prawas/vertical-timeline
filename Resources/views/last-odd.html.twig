<div class="row align-items-center justify-content-end vertical-left-right-timeline-text-row">
  <div class="col-6 text-right">
    <h5>{{ item.title }}</h5>
    <p>{{ item.description }}</p>
  </div>
  <div class="col-2 text-center top">
    <div class="circle">{{ item.number }}</div>
  </div>
</div>
